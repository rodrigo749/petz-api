// Placeholder migration file (for record). Prefer running scripts/run_migrations.js which
// safely adds columns if they don't exist.

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // See scripts/run_migrations.js for a safe, runtime-applied migration.
  },
  down: async (queryInterface, Sequelize) => {
    // Reverse operations would go here.
  }
};
